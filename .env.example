# pytaf environment variables
# Copy this file to .env and fill in your values.
# .env is excluded from git — never commit it.
#
# Generate a key with:
#   python scripts/manage_credentials.py generate-key

PYTAF_CREDENTIAL_KEY=your-fernet-key-here
