# ─────────────────────────────────────────────────────────
# jatefr Python Framework — configuration
# ─────────────────────────────────────────────────────────

# WebDriver
browser=chrome
headless=false
timeout=10

# Chrome remote-debug (attach to running browser instead of launching new)
# chrome.debug.address=localhost:9222

# Application under test
base.url=http://localhost:8080

# Reporting
report.name=myproject
report.title=MyProject

# API testing
# api.relaxed.https=false
# api.version=v1
# api.timezone=Europe/London

# Login page locators (CSS or XPath)
# login.username.selector=[name='username']
# login.password.selector=[name='password']
# login.button.selector=[type='submit']
# login.logout.selector=[data-testid='logout']
