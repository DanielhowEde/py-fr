# ─────────────────────────────────────────────────────────
# Example project — configuration
# ─────────────────────────────────────────────────────────

# Application under test
base.url=http://localhost:8080

# WebDriver
browser=chrome
headless=false
timeout=10

# Reporting
report.name=example
report.title=Example App

# Login page locators (CSS or XPath)
# login.username.selector=[name='username']
# login.password.selector=[name='password']
# login.button.selector=[type='submit']
# login.logout.selector=[data-testid='logout']

# API testing
# api.relaxed.https=false

# Credential store
# credential.file=credentials.enc
