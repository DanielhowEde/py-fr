# Core Portal test secrets
# Copy to .env and fill in values — .env is git-ignored

# Credential store encryption key (generate with: python scripts/manage_credentials.py generate-key)
PYTAF_CREDENTIAL_KEY=

# API token for admin user (obtain via POST /api/v1/auth/login)
COREPORTAL_API_TOKEN=
