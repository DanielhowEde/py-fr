# ─────────────────────────────────────────────────────────
# Core Portal — test configuration
# ─────────────────────────────────────────────────────────

# Application under test
base.url=http://18.134.7.166

# Browser
headless=false
timeout=10

# Reporting
report.name=coreportal
report.title=Core Portal Regression

# Login page locators
login.username.selector=input[type='email'],input[name='email']
login.password.selector=input[type='password'],input[name='password']
login.button.selector=button[type='submit']
login.logout.selector=[data-testid='logout'],#logout,.logout

# API
api.relaxed.https=false
